import { createTheme } from "@mui/material";

export const musatoriTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#E5EDE6",
    },
    background: {
      default: "#E5EDE6",
      paper: "#D2DDD3",
    },
    secondary: {
      main: "#D2DDD3",
    },
  },
});
