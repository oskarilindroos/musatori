import { createTheme } from "@mui/material";

export const musatoriTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#f5f5f5",
    },
    secondary: {
      main: "#8b7b74",
    },
  },
});
